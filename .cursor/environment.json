{
  "name": "fedidevs",
  "install": ".cursor/install.sh",
  "start": ".cursor/start.sh",
  "terminals": [
    {
      "name": "web",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; uv run python manage.py runserver 0.0.0.0:8000",
      "description": "Django development server on http://localhost:8000"
    },
    {
      "name": "tailwind",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; uv run python manage.py tailwind start",
      "description": "Tailwind CSS watcher that rebuilds theme styles on change"
    }
  ],
  "ports": [
    { "name": "web", "port": 8000 }
  ]
}
